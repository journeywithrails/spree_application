<div id="local_login">

  <div id="existing-customer">
    <h2>Existing Customers</h2>
    <p>If you've already registered for an account on Ezeesmoke™ you can sign in here:</p>
    <%= render :partial => 'shared/login' %>
  </div>

  <hr>

  <div id="new_customers">
    <h2>New Customers</h2>
    <p>You can register for a new account here on Ezeesmoke™ below:</p>
    <%= form_for(:user, :url => registration_path(User.new)) do |f| %>

        <% if session[:omniauth] %>
          <p>
            <%= f.label :email, t("email") %>
            <%= f.text_field :email, :class => 'title'  %>
          </p>
        <% else %>
          <%= render 'shared/user_form', :f => f %>
        <% end %>
        <p>
          <button type="submit" class="css3button-ligth-blue">Register</button>
        </p>
      <% end %>


  </div>
</div>